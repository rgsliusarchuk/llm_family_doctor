[Unit]
Description=LLM-Family-Doctor stack
After=docker.service
Requires=docker.service

[Service]
WorkingDirectory=/opt/familydoc
ExecStart=/usr/bin/docker compose up --pull=always -d
ExecStop=/usr/bin/docker compose down
Restart=always
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target 